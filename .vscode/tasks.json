{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build editor",
            "type": "shell",
            "command": "gcc main.c -g3 -o pged",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}